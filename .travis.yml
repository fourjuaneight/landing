language: node_js

os:
  - linux

dist: xenial

node_js:
  - 12.16.0

env:
  global:
    - KEY: {{CYPRESS_KEY}}

branches:
  only:
  - master

cache:
  directories:
  - node_modules

before_script:
  - npm ci

script:
  - npm run cypress run --record --key $KEY
  - npm run test:unit
